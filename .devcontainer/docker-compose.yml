version: "3.9"

services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/workspace
    env_file:
      - ../backend/.env
    command: /bin/sh -c "while sleep 1000; do :; done"