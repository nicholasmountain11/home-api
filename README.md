# Home API

API built using Python FastAPI, with the purpose of interacting with microcontrollers over wifi connection.

## Startup

cd backend/app

for running on local host, run
python3 main.py

for running on local ip address, run
HOST_MODE="network" python3 main.py
